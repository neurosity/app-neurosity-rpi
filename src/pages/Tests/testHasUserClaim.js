// import { getPossibleUserClaims } from "../../services/userData";

export function testHasUserClaim(test, userClaims) {
  return true;
  // if (!("userClaims" in test)) {
  // }

  // const possibleClaims = getPossibleUserClaims();

  // const hasClaims = possibleClaims.filter((claim) => {
  //   return userClaims?.[claim] ?? false;
  // });

  // return !!test.userClaims.filter((claim) => hasClaims.includes(claim)).length;
}
